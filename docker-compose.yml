services:
  postgres:
   image: postgres:18
   container_name: postgres
   ports:
    - "5434:5432"
   environment:
    POSTGRES_DB: flowmanager
    POSTGRES_USER: postgres
    POSTGRES_PASSWORD: postgres